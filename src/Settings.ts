// Typing


// Interface Creation
export interface LatexHeadingsSettings {
	version: string;
}

// Plugin default settings
export const DEFAULT_SETTINGS: LatexHeadingsSettings = {
	version: "0.1.0",
};
